# FounderOS Skill Pack Releases

Public distribution point for FounderOS install/upgrade tarballs.

Install: `curl -sSL https://founderos.ai/install.sh | bash -s <TOKEN>` (or clone directly from releases).

Source: private repo (Instabidsai/founderos). License: proprietary.
